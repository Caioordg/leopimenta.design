// Seleciona o elemento com o ID "Iniciar" (o botão) e adiciona um evento de clique a ele
document.getElementById("Iniciar").addEventListener("click", function() {
    // Quando o botão é clicado, a função anônima é executada
    window.location.href = "pg2.html"; // Redireciona o navegador para a página "pg2.html"
});