// Seleciona o botão com o ID "Iniciar" e adiciona um ouvinte de evento para cliques
document.getElementById("Iniciar").addEventListener("click", function() {
    // Quando o botão "Iniciar" é clicado, redireciona o navegador para a página "index.html"
    window.location.href = "index.html";
});

// Seleciona o botão com o ID "portifólio" e adiciona um ouvinte de evento para cliques
document.getElementById("portifólio").addEventListener("click", function() {
    // Quando o botão "portifólio" é clicado, redireciona o navegador para a página "pg3.html"
    window.location.href = "pg3.html";
});

// Seleciona o botão com o ID "mecontate" e adiciona um ouvinte de evento para cliques
document.getElementById("mecontate").addEventListener("click", function() {
    // Quando o botão "mecontate" é clicado, redireciona o navegador para a página "pg4.html"
    window.location.href = "pg4.html";
});